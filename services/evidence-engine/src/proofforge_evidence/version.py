"""Single source of the evidence engine version and the spec it emits."""

__version__ = "0.1.0"

# Version of the proof-manifest specification this engine produces.
# Must stay in step with packages/evidence-spec (SPEC_VERSION).
SPEC_VERSION = "1.1.0"
